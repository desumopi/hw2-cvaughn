package edu.cmu.deiis.annotators;

import edu.cmu.deiis.types.NGram;

public class AnswerSortAnnotator {

  
  
  
  
  
  //find the question's associated NGrams:
  // loop over the NGrams
  for (int n=0; n < ngArray.size(); i++) {
    NGram currNG = ngArray.get(n);
    if (currNG.getBegin() > i && currNG.getBegin() < k) {
      quest.
    }
  }
}
